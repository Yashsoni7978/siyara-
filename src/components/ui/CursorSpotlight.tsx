'use client'

/**
 * CursorSpotlight removed for performance optimization.
 * Radial gradient cursor spotlight follower removed to eliminate repaints and reduce main-thread CPU/GPU usage.
 */
export function CursorSpotlight() {
  return null
}
